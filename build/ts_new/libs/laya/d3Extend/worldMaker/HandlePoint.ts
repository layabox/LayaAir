export class HandlePoint {
		 x:number
		 y:number;
		 ctrlx:number;		// 控制点
		 ctrly:number;		// 控制点
		constructor(_x:number, _y:number, _ctrlx:number, _ctrly:number) {
			this.x = _x; this.y = _y; this.ctrlx = _ctrlx; this.ctrly = _ctrly;
		}
	}

	
	