package laya.d3.physicsCannon {
	import laya.d3.physicsCannon.CannonPhysicsTriggerComponent;

	/**
	 * <code>PhysicsCollider</code> 类用于创建物理碰撞器。
	 */
	public class CannonPhysicsCollider extends CannonPhysicsTriggerComponent {

		/**
		 * 创建一个 <code>PhysicsCollider</code> 实例。
		 * @param collisionGroup 所属碰撞组。
		 * @param canCollideWith 可产生碰撞的碰撞组。
		 */

		public function CannonPhysicsCollider(collisionGroup:Number = undefined,canCollideWith:Number = undefined){}
	}

}
