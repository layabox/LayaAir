/**
 * <code>SkyMesh</code> 类用于实现天空网格。
 */
export class SkyMesh {
    /**
     * 创建一个新的 <code>SkyMesh</code> 实例。
     */
    constructor() {
    }
    /**
     * @internal
     */
    _render(state) {
    }
}
